spring:
  datasource:
    url: jdbc:mysql://localhost:3306/disease_system?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.disease_server_system.entity
  configuration:
    map-underscore-to-camel-casetrue: true
